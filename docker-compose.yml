version: '3.8'

services:
  zip-ranker:
    build: .
    container_name: zip-ranker
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Europe/Paris
